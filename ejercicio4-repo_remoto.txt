- Crear un nuevo repo en GitHub llamado basic_repo
  	Se creó el repositorio en la pagina GitHub y se obtuvo el enlace para realizar el remoto.
- Crear un repo local también llamado basic_repo
  Crear algunos archivos dentro de tu repo y agrega algo de contenido en ellos
  Agregar, confirmar y empujar los cambios a GitHub		
	Se creó una carpeta llamada basic_repo y se accedió a Git Bash desde el interior de esta.
	En el Bash se creo un archivo index.html y se siguieron los pasos para agregarla al repositorio remoto. 
	git init; git add .; git commit -m "primer commit"; git remote add origin "enlace de basic_repo en GitHub"; git push;	
- Hacer algunos cambios más en la copia local y empujarlos a GitHub
	Se ingresó código al html y se actualizó el repositorio remoto
	git add .; git commit -m "comienzo del codigo"; git push;
- Ve a tu cuenta de GitHub y mira tu nuevo repo y los cambios que has hecho